# Coverage Evidence

Current checked-in reference:

- `backend-jacoco-summary.md`: latest aggregate coverage snapshot

Generate JaCoCo output or screenshots from:

```bash
cd backend
./mvnw test
```

The HTML report is generated under `/Users/patrick/Developer/REMS_DIGT3101/backend/target/site/jacoco/`.
